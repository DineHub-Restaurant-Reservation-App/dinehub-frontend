import { FormatBusinessHoursPipe } from './format-business-hours.pipe';

describe('FormatBusinessHoursPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatBusinessHoursPipe();
    expect(pipe).toBeTruthy();
  });
});
